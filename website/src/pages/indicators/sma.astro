---
import IndicatorLayout from '../../layouts/IndicatorLayout.astro';

const indicatorId = 'sma';
const indicatorName = 'Simple Moving Average';
const description = `Technical analysis indicator: Simple Moving Average`;
const parameters = [
  {
    "name": "period",
    "type": "number",
    "default": 14,
    "min": 2,
    "max": 200,
    "description": "Lookback period"
  }
];
---

<IndicatorLayout
  indicatorId={indicatorId}
  indicatorName={indicatorName}
  description={description}
  parameters={parameters}
>
  <h2>Overview</h2>
  <p>{description}</p>

  <h2>How it works</h2>
  <p>The Simple Moving Average calculates the arithmetic mean of prices over a specified period. It smooths out price action by creating a single flowing line that represents the average price over time.</p>

  <h2>Parameters</h2>
  <ul>
    <li><strong>period</strong>: Lookback period (default: 14)</li>
  </ul>

  <h2>Interpretation</h2>
  <p>- Price above SMA: Potential uptrend<br />- Price below SMA: Potential downtrend<br />- Price crossing SMA: Possible trend change</p>

  <h2>Example Usage</h2>
  <pre><code class="language-rust">
// Example of using Simple Moving Average in Rust
let result = indicators::sma(&data, 14);
  </code></pre>
</IndicatorLayout>