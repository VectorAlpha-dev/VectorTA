---
import IndicatorLayout from '../../layouts/IndicatorLayout.astro';

const indicatorId = 'bandpass';
const indicatorName = 'Band Pass Filter';
const description = `Technical analysis indicator: Band Pass Filter`;
const parameters = [
  {
    "name": "period",
    "type": "number",
    "default": 14,
    "min": 2,
    "max": 200,
    "description": "Lookback period"
  }
];
---

<IndicatorLayout
  indicatorId={indicatorId}
  indicatorName={indicatorName}
  description={description}
  parameters={parameters}
>
  <h2>Overview</h2>
  <p>{description}</p>

  <h2>How it works</h2>
  <p>The Band Pass Filter helps traders identify potential trading opportunities by analyzing price patterns and market conditions.</p>

  <h2>Parameters</h2>
  <ul>
    <li><strong>period</strong>: Lookback period (default: 14)</li>
  </ul>

  <h2>Interpretation</h2>
  <p>Traders use the Band Pass Filter to make informed decisions about entry and exit points.</p>

  <h2>Example Usage</h2>
  <pre><code class="language-rust">
// Example of using Band Pass Filter in Rust
let result = indicators::bandpass(&data, 14);
  </code></pre>
</IndicatorLayout>