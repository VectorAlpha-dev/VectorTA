---
import IndicatorLayout from '../../layouts/IndicatorLayout.astro';

const indicatorId = 'highpass_2_pole';
const indicatorName = '2-Pole High Pass Filter';
const description = `The 2-Pole High Pass Filter is an advanced signal processing technique that provides sharper frequency cutoff and better attenuation of low frequencies compared to the standard high pass filter, offering superior trend removal and cycle isolation.`;
const parameters = [
  {
    "name": "period",
    "type": "number",
    "default": 48,
    "min": 5,
    "max": 200,
    "description": "Cutoff period for the filter"
  }
];
---

<IndicatorLayout
  indicatorId={indicatorId}
  indicatorName={indicatorName}
  description={description}
  parameters={parameters}
>
  <h2>Overview</h2>
  <p>
    The 2-Pole High Pass Filter removes low-frequency components from price data to isolate short-term 
    cycles. With sharper attenuation than standard high-pass filters (-12 dB/octave roll-off), it provides 
    superior separation between trend and cycle components. The default 48-bar cutoff period passes cycles 
    shorter than 48 bars while strongly attenuating longer-term trends, making it ideal for cycle analysis 
    and identifying market turning points.
  </p>

  <h2>Parameters</h2>
  <ul>
    <li>
      <strong>period</strong> (number): Cutoff period in bars. Default: 48, Range: 5-200
      <ul>
        <li>Shorter periods (20-30) = aggressive filtering, only very short cycles pass</li>
        <li>Longer periods (60-125) = gentle filtering, more cycles preserved</li>
        <li>Must be ≥ 5 for stable results</li>
      </ul>
    </li>
  </ul>

  <h2>Returns</h2>
  <p>
    Array of filtered values representing detrended price movements:
  </p>
  <ul>
    <li><strong>Positive values:</strong> Price above trend</li>
    <li><strong>Negative values:</strong> Price below trend</li>
    <li><strong>Zero crossings:</strong> Potential cycle turning points</li>
    <li><strong>Initial values:</strong> 0.0 until filter stabilizes (≈2-3x period)</li>
  </ul>

  <h2>Example Usage</h2>
  <h3>Rust Implementation</h3>
  <pre><code class="language-rust"> // Rust implementation example will be added here
  </code></pre>

  <h3>Python Integration</h3> <pre><code class="language-python"> # Python implementation example will be added here
  </code></pre>

  <p class="text-sm text-muted-foreground mt-8"> Reference: Ehlers, J. F. (2013). <em>Cycle Analytics for Traders</em>. John Wiley & Sons.
  </p>
</IndicatorLayout>
