---
import IndicatorLayout from '../../layouts/IndicatorLayout.astro';

const indicatorId = 'rsx';
const indicatorName = 'Relative Strength Index - Smoothed';
const description = `Technical analysis indicator: Relative Strength Index - Smoothed`;
const parameters = [
  {
    "name": "period",
    "type": "number",
    "default": 14,
    "min": 2,
    "max": 200,
    "description": "Lookback period"
  }
];
---

<IndicatorLayout
  indicatorId={indicatorId}
  indicatorName={indicatorName}
  description={description}
  parameters={parameters}
>
  <h2>Overview</h2>
  <p>{description}</p>

  <h2>How it works</h2>
  <p>The Relative Strength Index - Smoothed helps traders identify potential trading opportunities by analyzing price patterns and market conditions.</p>

  <h2>Parameters</h2>
  <ul>
    <li><strong>period</strong>: Lookback period (default: 14)</li>
  </ul>

  <h2>Interpretation</h2>
  <p>Traders use the Relative Strength Index - Smoothed to make informed decisions about entry and exit points.</p>

  <h2>Example Usage</h2>
  <pre><code class="language-rust">
// Example of using Relative Strength Index - Smoothed in Rust
let result = indicators::rsx(&data, 14);
  </code></pre>
</IndicatorLayout>