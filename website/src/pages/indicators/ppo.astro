---
import IndicatorLayout from '../../layouts/IndicatorLayout.astro';

const indicatorId = 'ppo';
const indicatorName = 'Percentage Price Oscillator';
const description = `Technical analysis indicator: Percentage Price Oscillator`;
const parameters = [
  {
    "name": "fast_period",
    "type": "number",
    "default": 12,
    "min": 2,
    "max": 200,
    "description": "Fast period"
  },
  {
    "name": "slow_period",
    "type": "number",
    "default": 26,
    "min": 2,
    "max": 200,
    "description": "Slow period"
  }
];
---

<IndicatorLayout
  indicatorId={indicatorId}
  indicatorName={indicatorName}
  description={description}
  parameters={parameters}
>
  <h2>Overview</h2>
  <p>{description}</p>

  <h2>How it works</h2>
  <p>The Percentage Price Oscillator helps traders identify potential trading opportunities by analyzing price patterns and market conditions.</p>

  <h2>Parameters</h2>
  <ul>
    <li><strong>fast_period</strong>: Fast period (default: 12)</li>
    <li><strong>slow_period</strong>: Slow period (default: 26)</li>
  </ul>

  <h2>Interpretation</h2>
  <p>Traders use the Percentage Price Oscillator to make informed decisions about entry and exit points.</p>

  <h2>Example Usage</h2>
  <pre><code class="language-rust">
// Example of using Percentage Price Oscillator in Rust
let result = indicators::ppo(&data, 12, 26);
  </code></pre>
</IndicatorLayout>