---
title: "Weighted Moving Average"
description: "A moving average that assigns greater weight to more recent data points."
category: "moving_averages"
parameters:
  - name: "period"
    type: "number"
    default: 20
    min: 2
    max: 500
    description: "The number of periods"
returns:
  type: "Vec<Option<f64>>"
  description: "A vector of calculated Weighted Moving Average values"
complexity: "O(n)"
implementationStatus: "planned"
---

import { IndicatorLayout } from '../../../layouts/IndicatorLayout.astro';
import { CodeBlock } from '../../../components/ui/CodeBlock.astro';

<IndicatorLayout 
  indicatorId="wma" 
  indicatorName={frontmatter.title}
  description={frontmatter.description}
  parameters={frontmatter.parameters}
>

## Overview {#overview}

A moving average that assigns greater weight to more recent data points.

*Note: This is placeholder documentation. Full documentation will be added when the indicator is implemented.*

## Interpretation {#interpretation}

Details about how to interpret Weighted Moving Average values will be added here.

## Calculation {#calculation}

The mathematical formula and calculation steps for Weighted Moving Average will be documented here.

## Parameters {#parameters}

### period (default: 20)
The number of periods

- Type: `number`
- Min: 2
- Max: 500


## Returns & Output {#returns}

The Weighted Moving Average function returns a vector of optional floating-point values representing the indicator calculations.

## Example Usage {#usage}

<CodeBlock lang="rust">
use vectorta::indicators::wma;

// Example usage will be added when implemented
let result = wma(&data, 20)?;
</CodeBlock>

## Common Use Cases {#use-cases}

Common trading strategies and applications using Weighted Moving Average will be documented here.

## Edge Cases & Errors {#edge-cases}

Information about edge cases and error handling will be added here.

## References {#references}

Academic and industry references for Weighted Moving Average will be listed here.

</IndicatorLayout>