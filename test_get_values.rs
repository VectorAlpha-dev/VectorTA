// Quick test to get the actual values
use std::error::Error;

fn main() -> Result<(), Box<dyn Error>> {
    // Simulating the test to extract values
    println!("Run the actual test with --nocapture to get all values");
    Ok(())
}
